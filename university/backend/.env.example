# Option A: Full connection string (recommended). Keep it secret and DON'T commit.
# If your password contains special characters, URL-encode it (use encodeURIComponent).
# Example (password encoded): mongodb+srv://24577cs_db_user:Tharun%40%23*07@cluster0.6hq9oeu.mongodb.net/mydb?retryWrites=true&w=majority
# MONGO_URI="mongodb+srv://<user>:<encodedPassword>@<host>/<db>?retryWrites=true&w=majority"

# Option B (preferred for convenience; the app will encode user/pass automatically)
MONGO_USER=24577cs_db_user
MONGO_PASS=Tharun@#*07
MONGO_HOST=cluster0.6hq9oeu.mongodb.net
MONGO_DB=mydb

PORT=4000

JWT_SECRET=change-this-to-a-strong-secret

# Checklist if you see "bad auth : authentication failed":
# 1) Confirm the username & password in Atlas exactly match the values above.
# 2) In Atlas > Database Access verify the user exists and has the correct role for the DB.
# 3) In Atlas > Network Access add your IP (or 0.0.0.0/0 temporarily for testing).
# 4) Try the full MONGO_URI with the password URL-encoded if you still get auth errors.
